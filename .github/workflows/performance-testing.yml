name: Performance Testing

on:
  push:
    branches:
      - main
jobs:
  performance-testing:

    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
